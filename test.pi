id :: (x : Type) -> (y : x) -> x.
id = \x.\y.y.

idid :: (x : Type) -> (y : x) -> x.
idid = id.

true :: (A : Type) -> (x : A) -> (y : A) -> A.
true = \A.\x.\y.x.

false :: (A : Type) -> (x : A) -> (y : A) -> A.
false = \B.\x.\y.y.

